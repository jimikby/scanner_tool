You need to download TPD repository and gradle-plugins repository 
When you run the application, specify the path to the repository and TDP gradle-plugins. Also specify the path to the folder new TDP.
Specify component - you can specify *, tools, distrib*